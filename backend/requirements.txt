# Core scientific stack
numpy>=1.24
scipy>=1.10

# Mesh I/O (SU2, CGNS, Plot3D, VTK, ...)
meshio>=5.3
h5py>=3.8          # required by meshio for CGNS (HDF5)

# CAD / STEP import (OpenCASCADE bindings, pip wheels)
cadquery-ocp>=7.7

# Web API
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
python-multipart>=0.0.9

# Web API testing / client
httpx>=0.27
pytest>=7.0
